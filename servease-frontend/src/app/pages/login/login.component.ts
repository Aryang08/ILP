import { Component } from '@angular/core';import { FormsModule } from '@angular/forms';import { Router } from '@angular/router';import { AuthService } from '../../core/services/auth.service';import { CommonModule } from '@angular/common';
@Component({standalone:true,imports:[FormsModule,CommonModule],template:`<div class='container'><div class='card'><h2>Servease Login</h2><input [(ngModel)]='userId' placeholder='User ID'><br><br><input [(ngModel)]='password' type='password' placeholder='Password'><br><br><button (click)='doLogin()'>Login</button><p class='error' *ngIf='error'>{{error}}</p></div></div>`})
export class LoginPageComponent{userId='';password='';error='';constructor(private auth:AuthService,private router:Router){} doLogin(){this.auth.login(this.userId,this.password).subscribe({next:r=>{this.auth.saveSession(r);this.router.navigate(['/'+r.role.toLowerCase()]);},error:()=>this.error='Invalid credentials'});}}
